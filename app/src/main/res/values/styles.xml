<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="blackText" parent="AppTheme">
        <item name="android:textColor">@android:color/background_dark</item>
        <item name="android:editTextColor">@android:color/background_dark</item>
        <item name="android:buttonStyle">@style/myWhiteStyle</item>
    </style>

    <style name="whiteText" parent="AppTheme">
        <item name="android:textColor">@android:color/background_light</item>
        <item name="android:editTextColor">@android:color/background_light</item>
        <item name="android:buttonStyle">@style/myBlackStyle</item>
    </style>
    
    <style name="myBlackStyle" parent="@style/Widget.AppCompat.Button">
        <item name="android:background">@android:color/background_dark</item>
    </style>

    <style name="myWhiteStyle" parent="@style/Widget.AppCompat.Button">\
        <item name="android:background">@android:color/background_light</item>
    </style>

</resources>
